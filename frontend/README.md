# Frontend Setup

## Cài Đặt

1. Cài đặt dependencies:
```bash
npm install
```

2. Chạy development server:
```bash
npm start
```

Frontend sẽ chạy tại `http://localhost:3000`

## Cấu Hình

Nếu backend chạy ở port khác, tạo file `.env`:
```
REACT_APP_API_URL=http://localhost:5000/api
```

